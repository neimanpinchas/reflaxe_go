package tests;

//using Lambda;

function main() {
    for (i in 0...100) {
        trace(i);
    }
    for (i in 0...100) {
        trace(i);
    }
    // var aaa=({iterator:()->0...100}).array();
    // trace(aaa.map(v->v*2));
    // trace(aaa.map(v->2/v));
}
